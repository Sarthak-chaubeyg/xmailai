{
  "name": "xmailai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "XMailAI — AI-Powered Personalized News Intelligence",
  "scripts": {
    "dev": "npx netlify-cli dev",
    "build": "echo 'Static site — no build step required'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
